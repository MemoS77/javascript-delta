{
  "name": "javascript-delta",
  "version": "1.0.0",
  "description": "For GrandGames",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "dev": "npx http-server ./src -o index.html",
    "start": "npm run build && npx http-server ./dist -o index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "@web/rollup-plugin-html": "^2.1.2",
    "http-server": "^14.1.1",
    "parcel": "^2.11.0",
    "rollup": "^4.9.6",
    "rollup-plugin-copy": "^3.5.0"
  }
}
